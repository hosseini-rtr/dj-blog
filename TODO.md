## We have to manage how run in prod and local:

- one way: ENV_FILE=.env.production ./manage.py runserver --settings=core.settings.production
- Or: ./manage.py runserver --settings=core.settings.development

## Add font to blog
